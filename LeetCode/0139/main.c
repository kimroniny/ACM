#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>


bool wordBreak(char * s, char ** wordDict, int wordDictSize){
    int l = len(s);
    int d[l];
    int i, j;
    for (i = 0; i < l; i++)  {
        for (j = 0; j < i; j++) {
            
        }
    }
    return true;
}

int main() {
    printf("hello C !");
    return 0;
}